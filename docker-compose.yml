services:
  stock-alert-agent:
    build: .
    container_name: stock-alert-agent
    env_file:
      - .env
    environment:
      - CONFIG_PATH=/config/config.yaml
    ports:
      - "8000:8000"
    volumes:
      - ./:/config:ro
    restart: unless-stopped
